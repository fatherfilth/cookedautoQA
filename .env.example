# Required
BASE_URL=https://cooked.com

# Test credentials (optional — auth setup auto-registers per run)
# Auth setup project (tests/auth/auth.setup.ts) automatically registers a fresh
# disposable account using smoketest+{timestamp}@totempowered.com on each test run.
# These variables are kept for reference but are no longer required.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Alerting (Phase 5)
# Slack incoming webhook URL for failure notifications
# Create at: https://api.slack.com/messaging/webhooks
# Leave empty to skip Slack notifications (script will log warning and continue)
SLACK_WEBHOOK_URL=

# Display mode (set to "false" for headed mode)
HEADLESS=true

# Game IDs for monitoring
# Representative game IDs per category — inspect live site to determine actual values
# These are used in game launch tests (GAME-01, GAME-02, GAME-03)
GAME_SLOT_ID=starburst
GAME_SLOT_NAME=Starburst
GAME_TABLE_ID=blackjack
GAME_TABLE_NAME=Blackjack
GAME_LIVE_ID=live-roulette
GAME_LIVE_NAME=Live Roulette

# Crypto integration (Phase 4)
# Path to crypto buy page on cooked.com — inspect live site to confirm
CRYPTO_BUY_PATH=/crypto/buy
# Swapped.com iframe src pattern for detection
CRYPTO_IFRAME_SRC=connect.swapped.com
